(*s Synthesize SPS. *)

(*i*)
open Util
(*i*)

(* \hd{Generate polynomials} *)

(* \ic{Generate all $k$-dimensional vectors $w \in \mathbb{N}^k$ such
       that $k = dim(v)$ and $w_i \leq v_i$ for all $i$.} *)

let vecs_smaller v = []

(* \hd{Compute verification equation} *)

(* \hd{Analyze Scheme} *)

let synth () =
  F.printf "Not implemented yet.\n"
